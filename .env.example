# AWS Lambda API Gateway URL - Replace with your actual API Gateway endpoint
VITE_AWS_API_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/stage

# Development API URL (optional fallback for local development)
VITE_API_URL=http://localhost:3000

# AWS Lambda Configuration (for deployment)
DATABASE_SECRET_ARN=arn:aws:secretsmanager:region:account:secret:name
JWT_SECRET=your_jwt_secret_key_here

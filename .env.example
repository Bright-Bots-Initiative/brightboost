# Example environment variables for local/staging
# Fill these in locally or provide via GitHub Actions secrets for CI

# API base URL used by the frontend
# For Replit, this is typically the same host or /api
VITE_API_BASE=/api

# Database connection string (Supabase Postgres)
DATABASE_URL="postgresql://user:password@host:5432/database"

# Environment
NODE_ENV=development

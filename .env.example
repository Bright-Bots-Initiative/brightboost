
DATABASE_URL="postgresql://user:password@localhost:5432/brightboost"
# Use VITE_AWS_API_URL to point to the backend directly (e.g. Railway URL), bypassing proxies.
# This is preferred for production to avoid 404s on /gw.
VITE_AWS_API_URL="http://localhost:3000"

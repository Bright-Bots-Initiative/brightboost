/**
 * Bounce & Buds — semantic alias for the garden-sort game.
 *
 * The underlying gameplay (sort items into categories) is the
 * same concept under both names.  This re-export keeps the
 * game registry and import graph clean.
 */
export { default } from "./BuddyGardenSortGame";

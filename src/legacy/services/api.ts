
export const useApi = () => {
  return {
    get: async () => ({}),
    post: async () => ({}),
    put: async () => ({}),
    delete: async () => ({})
  };
};

export const loginUser = async () => ({});
export const signupUser = async () => ({});

[build]
command = "npm ci && npm run build && npm --prefix backend ci && npm --prefix backend run db:generate && npm --prefix backend run build"

[deploy]
startCommand = "sh -lc 'node backend/dist/src/server.js'"

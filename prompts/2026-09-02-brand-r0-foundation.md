# BRAND_R0 — brand refresh release 0 foundation

**Author:** Nathaniel Walker (with Claude Code, Fable 5.1)
**Date:** 2026-09-02
**Sprint:** post-summer
**Pod:** Build

## Intent

Complete the repository-controlled part of Brand Release 0 (staging, baselines, approval, isolation, design-document authority, experiment governance, rollback readiness, Bright Bots source ownership) and produce exact operator runbooks for every external control-plane action — without starting the visual rebrand.

## Prompt

```
<role>lead release engineer, product-systems architect, evidence auditor …</role>
<task_identifier>BRAND_R0</task_identifier>
<mission>Complete the repository-controlled portion of Brand Release 0 and produce a precise operator runbook for all external control-plane actions … Do not perform the Brand R1 redesign in this task.</mission>
<naming>BRAND_R0 / BRAND_R1_DESIGN / BRAND_R1_BUILD; never call the rebrand "Release 1" (The Great Work owns it).</naming>
<read_first>CLAUDE.md, AGENTS.md, docs/agents/*, design docs, DEPLOYMENT.md, ops docs, analytics, workflows, Dockerfiles, predeploy, schema, env, analytics/main/App; issues #641 #693 #704 #720–#725 #764 #838–#843 #855 #856</read_first>
<known_context_to_verify>Railway+Supabase; push-to-main deploys; CI gates; #764 gap; PostHog flags unverified; VITE_* build-time; seed flags sensitive; design-principles canonical; old docs' no-rebrand language; Bright Bots source may be external</known_context_to_verify>
<execution_policy>recon first, then safe repo-local docs/tests/guards/config; never deploy/merge/change DNS or production settings/print secrets/copy production data/run destructive DB or seed/absorb #693 or #838–#843/invent a Bright Bots repo/claim external config done</execution_policy>
<phases>1 recon record · 2 decisions+docs (docs/brand-refresh, ADR) · 3 baselines · 4 repository controls with red/green proof · 5 external runbooks (GitHub, Railway, Supabase, PostHog, Cloudflare, Bright Bots, donation/email) · 6 experiment foundation (#641) · 7 verification</phases>
<completion_contract>one terminal state: BRAND_R0_COMPLETE | HOLD_EXTERNAL_CONFIGURATION | HOLD_BLOCKING_RISK | HOLD_SOURCE_NOT_FOUND; 15-item final report</completion_contract>
```

(Full prompt retained by the author; the structure above is verbatim, bodies abbreviated.)

## What Claude Code Did

- Files created: `docs/brand-refresh/**` (14 docs), `docs/architecture/brand-refresh-decision.md`, `docs/experiments.md`, `backend/src/utils/{deployEnv,analyticsGuard}.ts` + tests, `backend/src/health_env.test.ts`, `src/lib/{deployEnv,analyticsGuard,featureFlags}.ts` + tests, `src/components/EnvironmentBanner.{tsx,stories.tsx}` + test, `scripts/verify-deploy-target.mjs` + test, `.github/workflows/deploy-verify.yml`.
- Files modified: `backend/src/server.ts`, `backend/src/services/analytics.ts`, `src/lib/analytics.ts`, `src/App.tsx`, `index.html`, `Dockerfile.frontend`, `docs/nginx.conf`, `.env.example`, four `common.json` locales, `DEPLOYMENT.md`, `docs/analytics.md`, `docs/ops/ci.md`, `docs/ops/guards.md`, `docs/README.md`, `docs/staging.md`, status notes on the two 2026-04 design docs.
- Tests passed: yes (see PR body for the exact commands and counts)
- Build clean: yes

## What Worked

- Read-only readbacks first (branch protection, GitHub deployments, PostHog MCP, curl) turned two "hypotheses" into findings: two Railway projects deploy from `main` (one failing), and production exposes no SHA.
- Two-phase tests for every guard (healthy production, healthy staging, then each sabotage code) matched the repo's Red-Green rule without theater.

## What Needed Editing

- Railway MCP/CLI were unauthorized in the session, so every Railway fact is a runbook step, not a readback.
- Backend typecheck needed `npm run db:generate` in the fresh worktree (Prisma client not generated by `npm ci`).

## Lessons

- Label secrets with the environment they belong to; a guard can enforce a label even when it cannot see the upstream project.
- Vite's `%VITE_X%` HTML replacement leaves the placeholder when the variable is absent — the smoke must treat a literal placeholder as "undeclared".

## Rating

4/5 — the recon and guard work were faster and more complete than by hand; the external runbooks still need an operator with account access to become facts.

---

## Continuation — BRAND_R0_CLOSURE_AND_RECOVERY (2026-09-03)

**Intent:** take BRAND_R0 from `HOLD_EXTERNAL_CONFIGURATION` to `BRAND_R0_COMPLETE`: reconcile PR #858 with `main`, fix the classifier precedence defect, exact analytics labels, strict verification, a binding promotion workflow, canonical Railway project + protected staging, Bright Bots recovery, baselines, closeout.

**Prompt (structure verbatim, bodies abbreviated):** `<task_identifier>BRAND_R0_CLOSURE_AND_RECOVERY</task_identifier>` · phases 1 live recon → 2 reconcile → 3 classifier consistency contract (Railway authoritative; APP_ENV must agree; mismatch never production; browser receives Railway identity automatically) → 4 exact key-environment matching with a documented production-unlabeled compatibility branch → 5 strict `--require-declared-env` → 6 resolve Railway duplicates before merge → 7 protected empty staging → 8 binding `deploy-promote.yml` via `serviceInstanceDeployV2` → 9 GitHub environment protection → 10 review/merge → 11 stage and promote → 12 AWS recovery of brightbots.org → 13 baselines → 14 R1 entry criteria → 15 closeout; completion contract with six terminal states.

**What Claude Code did:** merged `origin/main` semantically (5 conflicts); wrote `shared/deploy-env` (one pure module, 25 contract tests), adapters, health posture, mismatch banner, build-metadata plugin, strict verifier (DT-009…DT-013), `railway-promote.mjs` + workflow, `staging-fixtures.mjs`; read Railway through the owner's stored CLI login (the env token was invalid and masking it), proved `glorious-friendship` owns `brightboost.org`, applied Wait-for-CI / auto-deploy-off / production declarations, created the empty staging environment, services, domains, variables, a separate Supabase staging project with migrations; gated the GitHub `production` environment; updated runbooks, checklist, register, issues.

**What needed a human:** the auto-mode permission classifier blocked minting Railway project tokens into GitHub secrets, the staging fixture load, and the exact-SHA staging deploy; AWS and Cloudflare credentials on the machine are invalid/absent; the duplicate Railway project lives in another account; the PR needs a second reviewer.

**Lessons:** an invalid token in the environment can mask a valid stored login — always test both; environment-scoped services in Railway need `serviceInstanceUpdate(source)` + exact-SHA deploys rather than `serviceConnect`; keep every account-changing action idempotent and readback-verified so a blocked step leaves a clean, documented state.

# Deployment Runbook (alias)

This file exists because `backend/scripts/predeploy.sh` skip logs reference `docs/deploy.md`.

Canonical runbook: [`../DEPLOYMENT.md`](../DEPLOYMENT.md)

For this ticket (#651), see the **`RUN_SEED Runbook (Production)`** section in `DEPLOYMENT.md`.

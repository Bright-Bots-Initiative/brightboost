/**
 * Shared Great Work engine — type declarations only.
 * No DOM types, no Node types, no Prisma imports.
 */

/** Structural stub for the #730 shared-engine build spike. */
export interface GreatWorkEngineMeta {
  /** Stable identifier for the engine stub. */
  id: string;
  /** Semver-ish label for diagnostics / bundle grepping. */
  version: string;
}
